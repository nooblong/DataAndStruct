package sort;

public class InsertSort<T extends Comparable<T>> {
    public void straightInsertSort(T[] R, int size){
        int pos, j;
        T tmp;
        for (pos = 1; pos < size; pos++) {
            tmp = R[pos];
        }
    }
}
